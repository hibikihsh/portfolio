---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-xs text-gray-400">
  {
    date.toLocaleDateString("ja", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
